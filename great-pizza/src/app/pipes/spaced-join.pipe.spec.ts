import { SpacedJoinPipe } from './spaced-join.pipe';

describe('SpacedJoinPipe', () => {
  it('create an instance', () => {
    const pipe = new SpacedJoinPipe();
    expect(pipe).toBeTruthy();
  });
});
